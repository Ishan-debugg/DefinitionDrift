# DefinitionDrift — Phase 1 requirements
# Zero purchase — all free to use within Anthropic free tier

# Core LLM
anthropic>=0.40.0

# Agent orchestration
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=0.1.0  # persistent HITL state across restarts

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# SQL parsing + schema diffing
sqlglot>=23.0.0

# Embeddings (local, free, runs on CPU — highly recommended)
sentence-transformers>=3.0.0

# Multi-database connection abstraction (SQLite, Postgres, MySQL)
sqlalchemy>=2.0.0
# Optional dialect drivers — install whichever you need:
#   pip install psycopg2-binary       ← PostgreSQL
#   pip install pymysql               ← MySQL / MariaDB
#   pip install pyodbc                ← MS SQL Server

# Scheduling (HOTL drift digest)
apscheduler>=3.10.0

# MCP server
mcp>=1.0.0

# Utilities
python-dotenv>=1.0.0

# Dev / testing only
httpx>=0.27.0
slowapi>=0.1.9